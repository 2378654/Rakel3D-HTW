using UnityEngine;
//unused
/*
 * public class TrackerRakelPosition
 
{
    //  private Vector3 trackerPosition;
    
    //trackerPosition.x = GameObject.Find("RenderedRakel").transform.localPosition.x;
    //trackerPosition.y = GameObject.Find("RenderedRakel").transform.localPosition.y;
    //trackerPosition.z = GameObject.Find("RenderedRakel").transform.localPosition.z;
    public static Vector3 Get()
    {
        //return ScreenToWorld.Convert(Input.mousePosition); // tracker position
        //Debug.Log("Local Position: " + GameObject.Find("RenderedRakel").transform.localPosition);
        //Debug.Log("Position: " + GameObject.Find("RenderedRakel").transform.position);
        return ScreenToWorld.Convert(GameObject.Find("RenderedRakel").transform.position);
    }
}
*/